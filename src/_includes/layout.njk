<!DOCTYPE html>
<html lang="en">
<head>
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/Styles/main.css">
</head>
<body>
    <header>
        <nav class="navbar">
            <ul>
                {% for item in nav %}
                    <li>
                        <a href="{{ item.url }}" class="{% if item.title == title %}selected{% endif %}">
                            {{ item.title }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </nav>
    </header>
    <main>
        {{ content | safe }}
    </main>
</body>
</html>
